<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.pinterior.dao.CommentDao">

	<!-- 댓글 등록 -->
	<insert id="insert"
		parameterType="com.mycompany.pinterior.entity.Comment">
		<selectKey keyProperty="commentId" resultType="long"
			order="BEFORE">
			SELECT comment_seq.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO comments (comment_id, user_id, pin_id, comment_content,
		created_at)
		VALUES (#{commentId}, #{userId}, #{pinId},
		#{commentContent}, SYSDATE)
	</insert>

	<!-- 댓글 단건 조회 -->
	<select id="selectById" parameterType="long"
		resultType="com.mycompany.pinterior.entity.Comment">
		SELECT
		c.comment_id AS commentId,
		c.user_id AS userId,
		c.pin_id AS pinId,
		c.comment_content AS commentContent,
		c.created_at AS
		createdAt,
		c.updated_at AS updatedAt,
		u.nickname AS userNickname,
		u.profile_img AS userProfileImg
		FROM comments c
		JOIN users u ON
		c.user_id = u.user_id
		WHERE c.comment_id
		= #{commentId}
	</select>

	<!-- 댓글 수정 -->
	<update id="update"
		parameterType="com.mycompany.pinterior.entity.Comment">
		UPDATE comments
		SET comment_content = #{commentContent},
		updated_at = SYSDATE
		WHERE comment_id = #{commentId}
	</update>

	<!-- 댓글 삭제 -->
	<delete id="deleteById">
		DELETE FROM comments
		WHERE comment_id = #{commentId}
		AND user_id = #{userId}
	</delete>

	<!-- 댓글 목록 조회 -->
	<select id="selectByPinId"
		resultType="com.mycompany.pinterior.dto.CommentResponseDto">
		SELECT
		c.comment_id AS commentId,
		c.user_id AS userId,
		u.nickname AS userNickname,
		u.profile_img AS userProfileImg,
		c.comment_content AS content,
		c.created_at AS createdAt,
		c.updated_at AS updatedAt
		FROM comments c
		JOIN users u ON c.user_id = u.user_id
		WHERE c.pin_id = #{pinId}
		ORDER BY c.created_at ASC
		OFFSET #{offset} ROWS FETCH FIRST #{size} ROWS ONLY
	</select>

	<!-- 댓글 총 개수 조회 -->
	<select id="countByPinId" parameterType="long" resultType="int">
		SELECT COUNT(*) FROM comments WHERE pin_id = #{pinId}
	</select>
</mapper>