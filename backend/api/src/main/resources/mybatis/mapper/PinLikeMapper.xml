<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.pinterior.dao.PinLikeDao">

    <insert id="insert">
        INSERT INTO pin_like (user_id, pin_id, created_at)
        VALUES (#{userId}, #{pinId}, SYSDATE)
    </insert>

    <delete id="delete">
        DELETE FROM pin_like
        WHERE user_id = #{userId}
        AND pin_id = #{pinId}
    </delete>
    
    <delete id="deleteByPinId">
        DELETE FROM pin_like
        WHERE pin_id = #{pinId}
    </delete>

    <select id="countByUserIdAndPinId" resultType="int">
        SELECT COUNT(*)
        FROM pin_like
        WHERE user_id = #{userId}
        AND pin_id = #{pinId}
    </select>

    <select id="countByPinId" resultType="int">
        SELECT COUNT(*)
        FROM pin_like
        WHERE pin_id = #{pinId}
    </select>

</mapper>