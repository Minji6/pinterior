<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.pinterior.dao.PinDao">

    <insert id="insert" parameterType="com.mycompany.pinterior.entity.Pin">
        INSERT INTO pin (title, description, image_url, link_url, created_at)
        VALUES (#{title}, #{description}, #{imageUrl}, #{linkUrl}, SYSDATE)
    </insert>

</mapper>