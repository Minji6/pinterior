<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.pinterior.dao.TagDao">

    <select id="selectByTagName" parameterType="String"
        resultType="com.mycompany.pinterior.entity.Tag">
        SELECT tag_id, tag_name
        FROM tag
        WHERE tag_name = #{tagName}
    </select>

    <insert id="insert" parameterType="com.mycompany.pinterior.entity.Tag">
        <selectKey keyProperty="tagId" resultType="int" order="BEFORE">
            SELECT tag_seq.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO tag (tag_id, tag_name)
        VALUES (#{tagId}, #{tagName})
    </insert>

</mapper>